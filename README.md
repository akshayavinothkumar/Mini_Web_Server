# Mini_Web_Server
A simple HTTP web server implemented using Java TCP sockets. The server handles client requests and sends HTML responses, demonstrating basic HTTP communication and client-server architecture.

->How to run
1. Compile the server:
   javac SimpleHttpServer.java

2. Run the server:
   java SimpleHttpServer

3. Open browser and go to:
   http://localhost:8080
